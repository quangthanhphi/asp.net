<!-- Default.cshtml -->

@model List<Product>


<div class="new_arrivals">
    <div class="container">
        <div class="row">
            <div class="col text-center">
                <div class="section_title new_arrivals_title">
                    <h2>New Arrivals</h2>
                </div>
            </div>
        </div>
        <div class="row align-items-center">
            <div class="col text-center">
                <div class="new_arrivals_sorting">
                    <ul class="arrivals_grid_sorting clearfix button-group filters-button-group">
                        <li class="grid_sorting_button button d-flex flex-column justify-content-center align-items-center active is-checked" data-filter="*">all</li>
                        @foreach (var item in Model)
                        {
                            @await Component.InvokeAsync("MenuArrivalPartial",item.ProductCategoryId)

                        }
                         </ul>
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col">
                <div class="product-grid" data-isotope='{ "itemSelector": ".product-item", "layoutMode": "fitRows" }'>
                    @foreach (var item in Model)
                    {
                        <!-- Product 1 -->
                        <div class="product-item @item.ProductCategoryId">
                            <div class="product discount product_filter">
                                <div class="product_image">
                                    @{
                                        string[] imageUrls = item.Image?.Split(';');
                                        if (imageUrls != null && imageUrls.Length > 0)
                                        {
                                            <img src="@imageUrls[0]" alt="">
                                        }
                                    }
                                </div>
                                <div class="favorite favorite_left"></div>
                                <div class="product_bubble product_bubble_right product_bubble_red d-flex flex-column align-items-center"><span>-$@(item.Price - item.PriceSale)</span></div>
                                <div class="product_info">
                                    <h6 class="product_name"><a href="">@item.Title</a></h6>
                                    <div class="product_price">$@item.PriceSale<span>$@item.Price</span></div>
                                </div>
                            </div>
                            <div class="red_button add_to_cart_button"><a href="~/assets/#">add to cart</a></div>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>